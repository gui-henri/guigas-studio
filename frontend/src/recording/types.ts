/** Metadata shape of requestVideoFrameCallback, for reference and typing. */
export interface VideoFrameCallbackMetadata {
  presentationTime?: number;
  expectedDisplayTime?: number;
  width?: number;
  height?: number;
}
